{
  "name": "cannabis-wellness-journal",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-static.mjs",
    "start": "npm run build && python3 -m http.server 4173 -d dist",
    "check": "node --check src/app.js && node --check src/logic.js && node --check scripts/build-static.mjs",
    "test": "node --test"
  }
}
